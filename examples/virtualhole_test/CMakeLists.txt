INCLUDE_DIRECTORIES(${PROJECT_INCLUDE_DIR})
LINK_DIRECTORIES(${PROJECT_DIR}/lib)
IF(LIBFTD2XX_FOUND)
  BUILD_EXES(virtualhole_test_ftd2xx "main.c" "virtualhole_ftd2xx" FALSE "-DLIBFTD2XX" FALSE)
ENDIF(LIBFTD2XX_FOUND)
IF(LIBFTDI_FOUND)
  BUILD_EXES(virtualhole_test_libftdi "main.c" "virtualhole_libftdi" FALSE "-DLIBFTDI" FALSE)
ENDIF(LIBFTDI_FOUND)
